# Model card — travel SLM (multitask)

- **Release**: `registry://muno/travel/release/travel_slm_multitask/1`
- **Model**: `registry://muno/travel/model/travel_slm_multitask/1` (registry v1)
- **Eval**: `registry://muno/travel/eval/travel_slm_multitask/1`
- **Backend / recipe**: mlx / lora
- **Base model**: `qwen2.5-1.5b-instruct`
- **Role layout**: single_multitask (single multitask LoRA: extract + render)
- **Decoding**: JSON-schema / grammar-constrained (the Phase-1 lever)
- **Lineage**: dataset → model → eval → release

## Intended use

Drop-in for the travel domain's intent-extraction + widget-render passes (the two LLM calls the consuming playbook declares). Serving target: `cpu`.

## Eval metrics (vs the deterministic oracle floor)

| metric | value |
| :-- | --: |
| widget_schema_validity | 1.0000 |
| extract_schema_validity | 1.0000 |
| tool_vocab_validity | 1.0000 |
| render_intent_vocab_validity | 1.0000 |
| tool_match | 0.5625 |
| render_intent_match | 0.5625 |
| widget_type_match | 0.3750 |
| arg_fidelity | 0.5625 |
| slot_update_match | 0.6250 |

- **Gate**: FAIL — tool_match=0.5625 < target 0.9800; render_intent_match=0.5625 < target 0.9800; widget_type_match=0.3750 < target 0.9800; arg_fidelity=0.5625 < target 0.9500; slot_update_match=0.6250 < target 0.9500
- **Floor target**: schema validity 1.0 (widget + extract). The release holds the floor when both validity rows are 1.0000.
- **Candidate latency**: p50 9214.977 ms / p95 13068.496 ms

## Limitations

- Trained on the Phase-1 seed dataset (small, synthetic + replay-augmented). Coverage is bounded by the seed corpus; out-of-distribution turns fall back to schema-constrained safe outputs.
- Constrained decoding guarantees *schema validity*, not *semantic correctness* — an in-vocab but wrong tool/intent is still possible; the match metrics above bound that.

_Generated by `slm_package.py` on unix 1782523756._
